package Day03;

public class If_else_statement {
	public static void main(String[] args) {
		int number = -95;
		if (number >= 0) {
			System.err.println(number + " is non-negative");
		}else {
			System.err.println(number + " is negative");
		}
	}
}
